# 🧠 Практичні завдання: **Цикли в JavaScript**

Мета цієї роботи — закріпити знання про різні типи циклів у JavaScript та навчитися застосовувати їх у практичних задачах.

---

## 🔁 Завдання 1: Парні числа (`for`)

**Умова:**
Напишіть цикл `for`, який виводить у консоль усі парні числа від **2 до 20**.

**Підказка:**

* Використовуйте оператор `%` для перевірки на парність
  **або**
* Змініть крок ітерації (наприклад, `i += 2`)

---

## ⏳ Завдання 2: Зворотний відлік (`while`)

**Умова:**
Використовуючи цикл `while`, реалізуйте зворотний відлік від **10 до 1**.

Після завершення циклу виведіть у консоль повідомлення:

```
"Старт!"
```

---

## ➕ Завдання 3: Сума чисел (`do..while`)

**Дано масив:**

```javascript
const numbers = [5, 12, 8, 0, 10];
```

**Умова:**
Напишіть програму, яка за допомогою циклу `do..while`:

* перебирає елементи масиву
* додає їх до загальної суми
* зупиняється, якщо зустрінеться число `0` або масив закінчиться

Виведіть підсумкову суму в консоль.

---

## 🔠 Завдання 4: Модифікація масиву (`for`)

**Дано масив:**

```javascript
const users = ["ivan", "olga", "petro", "anna"];
```

**Умова:**
Напишіть цикл, який змінює кожен елемент масиву так, щоб ім'я починалося з великої літери.

**Очікуваний результат:**

```javascript
["Ivan", "Olga", "Petro", "Anna"]
```

Виведіть змінений масив у консоль.

---

## 🔎 Завдання 5: Пошук у масиві (`break`)

**Умова:**

1. Створіть масив чисел.
2. Напишіть цикл, який перебирає масив і шукає число `7`.
3. Якщо число знайдене:

   * виведіть у консоль `"Знайдено!"`
   * негайно зупиніть цикл за допомогою `break`.

---

## ⏭ Завдання 6: Фільтрація значень (`continue`)

**Умова:**

Напишіть цикл від **1 до 15**.

* Використовуйте `continue`, щоб пропустити числа, які діляться на `3` без остачі.
* Решту чисел виведіть у консоль.

---

## 🚗 Завдання 7: Робота з об'єктом (`for..in`)

**Створіть об'єкт:**

```javascript
const car = {
  brand: "Toyota",
  model: "Camry",
  year: 2022,
  color: "black"
};
```

**Умова:**
За допомогою циклу `for..in` виведіть у консоль назву кожної властивості та її значення у форматі:

```
Ключ: Значення
```

---

## 💰 Завдання 8: Сума елементів масиву (`for..of`)

**Дано масив:**

```javascript
const prices = [150, 200, 50, 430, 100];
```

**Умова:**
За допомогою циклу `for..of`:

* порахуйте загальну суму цін
* виведіть результат у консоль

---

## 🔤 Завдання 9: Перетворення рядка (`for..of`)

**Дано рядок:**

```javascript
let message = "JavaScript";
```

**Умова:**
Напишіть цикл, який:

* виводить кожен символ рядка в окремому рядку консолі
* додає дефіс `-` після кожної літери

**Приклад виводу:**

```
J-
a-
v-
a-
S-
c-
r-
i-
p-
t-
```

---

## 🏆 Завдання 10: Пошук максимального числа

**Умова:**

1. Створіть масив випадкових чисел.
2. Використовуючи будь-який цикл:

   * знайдіть найбільше число в цьому масиві
   * виведіть його у консоль.

---

# ✅ Рекомендації

* Не використовуйте готові методи масиву (`map`, `reduce`, `filter`) — мета завдання саме в роботі з циклами.
* Намагайтеся писати чистий та читабельний код.
* Додавайте коментарі там, де це доречно.

---

💡 **Бонус:**
Спробуйте для кожного завдання реалізувати альтернативне рішення з іншим типом циклу.
