# 📘 Домашнє завдання №3  
## Operators · Type Conversions · Arrays · Conditional Statements

---

## 🎯 Мета

Закріпити навички роботи з:
- арифметичними операторами
- пріоритетом операторів
- порівняннями та логічними операторами
- приведенням типів
- масивами
- умовними конструкціями

---

# 🧩 Практичні завдання

---

## 1️⃣ Арифметичні оператори

Створіть змінні:

```js
let a = 15;
let b = 4;
````

Виведіть у консоль:

* суму
* різницю
* добуток
* частку
* остачу від ділення
* результат піднесення `a` до степеня 2

---

## 2️⃣ Інкремент та декремент

```js
let x = 5;
```

1. Використайте префіксний інкремент.
2. Використайте постфіксний інкремент.
3. Використайте префіксний декремент.
4. Поясніть різницю між префіксною та постфіксною формами.

---

## 3️⃣ Пріоритет операторів

Обчисліть результат:

```js
let result1 = 2 + 3 * 4;
let result2 = (2 + 3) * 4;
let result3 = 10 - 5 + 2;
```

Поясніть порядок виконання операцій.

---

## 4️⃣ Робота зі строками та приведення типів

Передбачте та перевірте результат:

```js
console.log(5 + "2");
console.log("5" - 2);
console.log("10" + true);
console.log("10" - true);
```

Поясніть, чому результати різні.

---

## 5️⃣ Порівняння та строга рівність

Перевірте результати:

```js
console.log(false == 0);
console.log(false === 0);
console.log("1" == 1);
console.log("1" === 1);
console.log(null == undefined);
console.log(null === undefined);
```

Поясніть різницю між `==` та `===`.

---

## 6️⃣ Логічні оператори

1. Перевірте значення:

```js
console.log(0 || "Hello" || 5);
console.log(1 && 0 && 5);
console.log(null || undefined || "JS");
console.log(2 || 1 && 0);
```

2. Напишіть умову, яка перевіряє:

   * чи число входить у діапазон від 0 до 100 включно
   * чи число знаходиться поза цим діапазоном

---

## 7️⃣ Тернарний оператор

Створіть змінну `age`.

Реалізуйте через тернарний оператор:

* якщо `age >= 18` → "Adult"
* інакше → "Minor"

---

## 8️⃣ Конвертація типів

1. Перетворіть:

   * `"25"` у число (двома способами)
   * `"25.5px"` у ціле число
   * `"25.5px"` у дробове число

2. Перевірте через `isNaN()`:

```js
isNaN("123");
isNaN("123abc");
isNaN(true);
isNaN(undefined);
```

Поясніть результати.

---

## 9️⃣ Робота з масивами

Створіть масив:

```js
const cities = ["Rome", "Lviv", "Warsaw"];
```

Виконайте:

1. Замініть перший елемент.
2. Додайте елемент у кінець масиву.
3. Додайте елемент на початок.
4. Видаліть останній елемент.
5. Виведіть довжину масиву.
6. Створіть новий масив, використовуючи `slice()`.
7. Використайте `map()` для створення нового масиву з довжиною назв міст.

---

## 🔟 Умовні конструкції

Створіть змінну `number`.

Реалізуйте перевірку:

* якщо число > 0 → "Positive"
* якщо < 0 → "Negative"
* якщо = 0 → "Zero"

Реалізувати:

1. Через `if...else if...else`
2. Через `switch`
3. Через тернарний оператор

---

# 📌 Вимоги до виконання

* Весь код повинен бути оформлений в одному `.js` файлі
* Додати коментарі з поясненнями
* Код повинен бути структурований та читабельний
* Завантажити рішення в окрему гілку GitHub
* Створити Pull Request

---

# 🏆 Додаткове завдання (опціонально)

Напишіть невелику програму:

Користувач вводить число через `prompt()`.

Програма:

* перевіряє, чи це число
* визначає, парне воно чи непарне
* перевіряє, чи входить у діапазон 1–100
* виводить результат у консоль

---
