# 📘 Домашнє завдання №2  
## Data Types · Type Conversions · Arrays

---

## 🎯 Мета роботи

Закріпити знання з тем:
- типи даних у JavaScript
- динамічна типізація
- приведення типів
- пріоритет операторів
- масиви та методи масивів

---

# 🧩 Практичні завдання

---

## 1️⃣ Типи даних

1. Створіть змінні різних типів:
   - String
   - Number
   - BigInt
   - Boolean
   - Undefined
   - Null
   - Object
   - Symbol

2. Виведіть у консоль значення кожної змінної та її тип за допомогою `typeof`.

---

## 2️⃣ Динамічна типізація

Створіть змінну `x`.

1. Присвойте їй значення `undefined`.
2. Потім число `45`.
3. Потім рядок `"45"`.
4. Після кожного присвоєння виводьте значення та тип.

Поясніть, що означає "динамічна типізація".

---

## 3️⃣ Перетворення у String

1. Перетворіть число `100` у рядок двома способами:
   - через `String()`
   - через додавання порожнього рядка

2. Перевірте тип результату.

---

## 4️⃣ Перетворення у Number

1. Перетворіть рядок `"250"` у число:
   - через `Number()`
   - через унарний `+`

2. Використайте:
   - `parseInt("12.75px")`
   - `parseFloat("12.75px")`
   - `parseInt("text")`

3. Поясніть результати.

---

## 5️⃣ Перевірка на число

Перевірте значення за допомогою `isNaN()`:

```js
isNaN("123");
isNaN("123abc");
isNaN(null);
isNaN(undefined);
isNaN(true);
````

Поясніть кожен результат.

---

## 6️⃣ Перетворення у Boolean

1. Перевірте результат:

```js
Boolean("Hello");
Boolean("");
Boolean(0);
Boolean(1);
Boolean(null);
Boolean(undefined);
```

2. Використайте подвійне заперечення `!!` для тих самих значень.

3. Поясніть, які значення є falsy.

---

## 7️⃣ Пріоритет операторів

Обчисліть та поясніть результат:

```js
console.log(10 - 20 / 5);
console.log(2 ** 4 * 2);
```

Змініть порядок виконання за допомогою дужок.

---

## 8️⃣ Робота з масивами (базові операції)

Створіть масив з 4 елементів (рядки або числа).

1. Виведіть перший та останній елемент.
2. Замініть другий елемент.
3. Додайте новий елемент з індексом 6.
4. Виведіть довжину масиву.
5. Переберіть масив:

   * через `for`
   * через `for...of`

---

## 9️⃣ Методи масивів

Створіть масив чисел:

```js
const numbers = [2, 4, 6, 8, 10];
```

1. Додайте число в кінець (`push`).
2. Видаліть останній елемент (`pop`).
3. Видаліть перший елемент (`shift`).
4. Додайте елемент на початок (`unshift`).
5. Використайте `slice()` для створення нового масиву.
6. Використайте `splice()` для видалення елемента за індексом.

---

## 🔟 Методи map, filter, reduce

Створіть масив:

```js
const numbers = [5, 10, 15, 20, 25];
```

1. За допомогою `map()` створіть новий масив, де кожне число помножене на 2.
2. За допомогою `filter()` отримайте числа більше 12.
3. За допомогою `reduce()` знайдіть суму всіх елементів.

---

# 📌 Вимоги до виконання

* Рішення оформити в одному `.js` файлі
* Додати коментарі з поясненнями
* Код має бути структурований та читабельний
* Завантажити рішення у GitHub окремою гілкою
* Створити Pull Request

---

# ⭐ Додаткове завдання (опціонально)

Створіть програму, яка:

1. Приймає масив чисел
2. Перевіряє, чи всі елементи є числами
3. Обчислює:

   * суму
   * середнє значення
   * мінімальне та максимальне значення

---
